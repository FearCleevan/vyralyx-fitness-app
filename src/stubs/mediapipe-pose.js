// Stub for @mediapipe/pose — browser-only package imported by
// @tensorflow-models/pose-detection's BlazePose MediaPipe detector.
// We only use MoveNet, so this stub is never executed at runtime.
module.exports = {};
